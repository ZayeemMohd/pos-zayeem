export const metadata = {
  title: "Inventory Manger - Dashboard",
  description: "",
};

export default function RootLayout({ children }) {
  return (
    <>
        {children}
    </>
  );
}
